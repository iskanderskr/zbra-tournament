import styled from 'styled-components'

import { Layout } from 'antd'

export const Header = styled(Layout.Header)`
	width: 100%;
	padding: 10px 0;
	height: auto;
	text-align: center;
	& h1 {
		margin: 0;
	}
`
